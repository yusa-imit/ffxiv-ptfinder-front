import ReactCSSTransitionGroup from "react-transition-group";
export default function TransitionIn(): JSX.Element {
  return (
    <ReactCSSTransitionGroup.CSSTransition
      transitionName={{}}
    ></ReactCSSTransitionGroup.CSSTransition>
  );
}
